# Hunt standup — 2026-06-08 (PR #34 revalidation, cab225f)

Full-sweep hunt: all 58 queued tasks dispatched (specialist subagents, waves of 3–4). 21 findings drafted, 37 ruled out. 0 pending.

## Revalidation verdict on prior findings
- **F001 (prior Critical, unsigned slashing evidence): RESOLVED.** `actor.rs::InboundEvidence` now gates on `verify_evidence_signatures`; signer_indices+epoch are signature-bound (internal F153); single verified store writer, no bypass (H001 ruled out, H014/H015 corroborate signed-payload coverage except F015).
- **F002 (prior High, lock admission skips signature/balance): STILL VULNERABLE → F035.** `HyperLockEvent` mints arbitrary amount into the threshold-signed verkle root with no balance closure, no range proof, and `lock_signature` is read by zero production code. Fix incomplete.

## Findings by severity
- **Critical (1):** F028 — DKLS threshold hard-pinned to 1 (`main.rs:1603`); one validator controls the group key.
- **High (12):** F002 (cross-epoch false-slash of innocent single-epoch signers), F003 (ring-vouch sybil EigenTrust amplification), F009 (signature-inclusive conflict hash mis-slashes honest committee on benign re-sign), F012 (committed block hash never re-derived from header → signed value decoupled), F013 (`FullProposal.height().unwrap()` remote DoS), F016 (unbounded `pending_dkls_inbound` epoch keys → memory DoS), F024 (buffered DKG drain skips F018 sender auth), F025 (committee index grinding via attacker-chosen validator_key), F035 (HyperLockEvent mint w/o balance closure), F045 (universal control-plane sig cross-deployment replay), F047 (owner-rotation front-run defeats key-compromise recovery), F049 (watermark saturation bricks rotate/cancel while permissionless executeUpgrade survives).
- **Medium (7):** F011 (shard read-validator skips protocol-version enforcement), F015 (slashing-store `encode_block` drops 6 signed fields → evidence not self-verifying), F018 (DKLS share keystore never pruned/zeroized), F021 (DKLS sender binding fails open on empty registered peer-id), F022 (FullProposal/DecidedValue gossip arms lack per-variant size cap), F039 (admin `retry_*` RPCs missing auth guard), F048 (`proposeUpgrade` not pause-gated; collapses cancel lockout window).
- **Low (1):** F036 (ConfidentialLockBody.range_proof accepted but never verified; impact foreclosed by public amount + Pedersen closure).

## Clusters flagged for Dedupe
- Bridge watermark cluster: F045 / F047 / F048 / F049 all hinge on the single shared 64-bit `latestBlock` watermark gating heterogeneous universal control-plane ops.
- Slashing false-positive cluster: F002 (cross-epoch) and F009 (signature-inclusive hash) are distinct root causes but same blast surface (honest-validator eviction); F015 adjacent (evidence re-encode fidelity).
- DKLS sender-auth: F021 and F024 both concern the F018 sender/peer-id binding (fail-open vs buffer-bypass).

All hunters confirmed `code/` tree untouched (read-only).
