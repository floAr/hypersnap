# Recon Standup — 2026-06-08

**Target:** hypersnap @ `cab225f1f63aea650201a4c0dfbb06a8cbbbb7b9` (branch `pow`, PR #34 "proof of work (restored)")
**Library SHA:** `b2c8f8bade0bf4b91d254eb4d5774b7fd3e3c1ea`
**Run type:** Revalidation (prior audit @ `64493318…` found F001/F002; PR #34 claims fixes)

## Recipes applied (all 7 matched)
| Recipe | Signal in target | Activated |
|---|---|---|
| rust-cargo-workspace | `Cargo.toml` workspace; fanout: `rocksdb`→http-api-rocksdb, `ed448-bulletproofs`→bulletproofs | (fanout) |
| libp2p-presence | `libp2p` + `gossipsub` in `[dependencies]` | p2p-gossip |
| tendermint-malachite-presence | `informalsystems-malachitebft-{core-consensus,engine,...}` path deps | consensus-malachite-tendermint, node-lifecycle-actor |
| threshold-ecdsa-presence | `dkls23` dep + `src/hyper/dkls_*` integration | rust-threshold-signing, rust-crypto-primitives |
| bridge-detection | `bridge_payload.rs`, `bridge_state.rs`, `HypersnapBridge.sol`, bridge-ceremony crate | solidity-bridge, rust-crypto-primitives |
| solidity-foundry | `contracts/foundry.toml` + `HypersnapBridge.sol` (UUPS, ERC20Permit) | evm-state-machine, solidity-bridge, solidity-proxy-access, evm-tokens |
| validator-economics-presence | `src/hyper/slashing*.rs`, `src/emission/**`, `scoring.rs`, `eigentrust.rs`, proof-of-quality crate | chain-economics |

## Specialists activated (12)
chain-economics, consensus-malachite-tendermint, http-api-rocksdb,
node-lifecycle-actor, p2p-gossip, rust-bulletproofs-pedersen,
rust-crypto-primitives, rust-threshold-signing, evm-state-machine,
evm-tokens, solidity-bridge, solidity-proxy-access.

Copied to `.claude/agents/specialists/<name>.md`.

## Bespoke specialists (0)
No substantial uncovered domain. Verkle/KZG primitives fall under
rust-crypto-primitives + rust-bulletproofs-pedersen; confidential-token
balance-closure under rust-bulletproofs-pedersen. None warranted.

## Hunt queue
**58 pending tasks** (`H001`–`H058`) in `.audit/hunt-queue.jsonl`.
Per specialist: chain-economics 8, solidity-bridge 7, rust-crypto-primitives 6,
consensus 5, http-api-rocksdb 5, node-lifecycle-actor 5, p2p-gossip 5,
rust-threshold-signing 5, rust-bulletproofs-pedersen 4, solidity-proxy-access 4,
evm-state-machine 2, evm-tokens 2.

## Revalidation findings located (for hunt to confirm)
- **F001 fix site** (`actor.rs` InboundEvidence): now runs
  `detect_conflicting_blocks` + `verify_evidence_signatures` before
  `record_evidence`. Open: does the epoch-boundary *enforcement* reader
  re-verify, or trust persisted `signer_indices`? F026 cross-epoch path is new.
  → tasks H001, H002, H014.
- **F002 likely INCOMPLETE**: `HyperLockEvent.lock_signature` exists but is
  **never verified** in production. `mempool::submit_lock`→`validate_lock_event`
  is structural only; producers zero-fill `lock_signature`. `import_hyper_block`
  verifies the *block* threshold sig + state-root but no per-lock sig.
  Authenticity rests on an honest proposer. → tasks H033, H013.

## Docs written
`docs/00-OVERVIEW.md`, `docs/architecture.md`, `docs/attack-surface.md`.

## Bookkeeping
`.audit/state.json`: `stages.recon = complete`;
`library.artifacts_in_use` populated with sha256 of 24 library files read.

## Open questions / BLOCKED
- None blocking. Vendored `crates/dkls23`, `crates/ed448-bulletproofs`, and
  `../malachite/**` are out of scope (integration-only review).
- Hunt should resolve whether PR #34's F002 fix added any lock-signature
  verification at all, or only re-verified the block signature.
